spring:
  datasource:
    url: jdbc:mysql://mysql.db.middle.jemmyweb.com:3306/jemmycloud_zipkin?characterEncoding=utf8&useSSL=false
    schema: classpath:/mysql.sql
  sleuth:
    enabled: false
  zipkin:
    locator:
      discovery:
        enabled: true

zipkin:
  collector:
    rabbitmq:
      addresses: paascloud-mq-rabbit:5672
      username: guest
      password: guest
      virtual-host: jemmyweb
      queue: zipkin
  storage:
    type: mysql

security:
  oauth2:
    client:
      clientId: client-infras-monitor
      clientSecret: oauth2ClientSecret
      access-token-uri: http://api.infras.jemmyweb.com:6030/uac/oauth/token
      user-authorization-uri: http://api.infras.jemmyweb.com:6030/uac/oauth/authorize
    resource:
      user-info-uri: http://api.infras.jemmyweb.com:6030/uac/user
      prefer-token-info: false
      jwt:
        key-uri: http://user.service.jemmyweb.com:6020/oauth/token_key

jemmycloud:
  oauth2:
    client:
      id: ${security.oauth2.resource.id}
      accessTokenUrl: ${security.oauth2.client.access-token-uri}
      clientId: ${security.oauth2.client.clientId}
      clientSecret: ${security.oauth2.client.clientSecret}
      clientAuthenticationScheme: header
      tokenStore: jwt